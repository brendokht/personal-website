---

---

<form
  name="contact"
  action="/success/"
  method="POST"
  data-netlify-recaptcha="true"
  data-netlify="true"
  class="flex min-h-fit w-[90%] max-w-lg flex-col justify-evenly gap-2 rounded-lg bg-white px-8 py-6 sm:w-3/4 dark:bg-black"
>
  <div class="hidden">
    <label>
      Don’t fill this out if you’re human: <input name="bot-field" />
    </label>
  </div>
  <div class="flex flex-col">
    <label for="name">Name</label>
    <input
      class="my-2 h-6 rounded-md border border-black px-2 py-4 focus:outline-purple-700 dark:border-white dark:bg-black dark:text-white"
      type="text"
      required
      name="name"
      placeholder="John Smith"
    />
  </div>
  <div class="flex flex-col">
    <label for="email">Email</label>
    <input
      class="my-2 h-6 rounded-md border border-black px-2 py-4 focus:outline-purple-700 dark:border-white dark:bg-black dark:text-white"
      type="email"
      required
      name="email"
      placeholder="johnsmith@example.com"
    />
  </div>
  <div class="flex flex-col">
    <label for="message">Message</label>
    <textarea
      class="my-2 h-6 min-h-48 rounded-md border border-black px-2 py-2 focus:outline-purple-700 dark:border-white dark:bg-black dark:text-white"
      name="message"
      required
      placeholder="Hey Brenden!"></textarea>
  </div>
  <div data-netlify-recaptcha="true"></div>
  <div class="mt-4 flex justify-center">
    <button
      class="rounded-lg bg-linear-to-br from-purple-700 to-fuchsia-700 px-9 py-1 text-white transition duration-300 hover:scale-105"
      type="submit">Submit</button
    >
  </div>
</form>
