---

---

<a href="#">
    <section
        class="w-72 min-h-72 bg-white p-2 rounded-md flex flex-col hover:scale-110 cursor-pointer duration-500 ease-in-out"
    >
        <div class="w-fit">
            <slot name="image" />
        </div>
        <div class="py-1 text-lg font-semibold">
            <slot name="title" />
        </div>
        <div class="flex-grow text-neutral-500 text-sm">
            <slot name="description" />
        </div>
    </section>
</a>
