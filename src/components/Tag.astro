---
import type { TagType } from "../types/tag-type";

interface Props extends TagType {}

const { text } = Astro.props;
---

<article class="not-prose bg-muted p-3 rounded-lg w-32">
  <p class="text-sm text-center">{text}</p>
</article>
